# Python cache files
__pycache__/
*.pyc

# Jupyter Notebook checkpoints
.ipynb_checkpoints/

# pytest cache
.pytest_cache/

# vscode
.vscode/

# macOS
.DS_Store

# downloaded and intermediate data (reproducible via the workflow)
data/raw_weather/
data/processed/

# saved workflow output: the joined accident-station table
# (regenerated by the workflow, too large for git)
data/joined/

# live pipeline outputs — regenerated on every run.
# The committed demo_results/ folder holds a frozen showcase snapshot instead.
results/

# wetterdienst's local cache (parquet files of fetched observations)
.wetterdienst/

# Snakemake working directory
.snakemake/

# Python virtual environments
venv/
.venv/
